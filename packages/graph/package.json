{
  "name": "graph",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "npm run build:supergraph && npm run build:codegen",
    "build:codegen": "graphql-codegen",
    "build:supergraph": "node scripts/config-gen.mjs && node scripts/prepare-schemas.mjs && rover supergraph compose --config config/supergraph.yaml > tmp/supergraph.graphql",
    "dev": "./tmp/router --dev --supergraph tmp/supergraph.graphql",
    "clean": "rm -rf tmp/*",
    "postinstall": "npm run build",
    "local-router": "curl -sSL https://router.apollo.dev/download/nix/latest | sh && mv router tmp/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Yeats <ay@xadamy.xyz>",
  "license": "ISC",
  "devDependencies": {
    "@apollo/rover": "^0.14.1",
    "@graphql-codegen/cli": "^3.3.0",
    "@graphql-codegen/typescript": "^3.0.3",
    "@graphql-codegen/typescript-resolvers": "^3.2.0"
  },
  "dependencies": {
    "graphql": "^16.6.0",
    "yaml": "^2.2.1"
  }
}
